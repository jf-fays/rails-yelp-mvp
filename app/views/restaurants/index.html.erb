<h1>List of Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant.id) %></li>
  <% end %>
</ul>
<div class="btn btn-warning"><%= link_to 'New restaurant', new_restaurant_path %></div>
</div>
